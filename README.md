# Binary Search Roll Finder

This is a simple C program that performs a binary search on a sorted list of student roll numbers to find a specific roll number.

## 🔍 Features
- Accepts roll numbers in ascending order
- Uses recursive binary search
- Displays the position and index of the found roll number

## 🛠️ How to Compile and Run

```bash
gcc binary_search.c -o rollfinder
./rollfinder
